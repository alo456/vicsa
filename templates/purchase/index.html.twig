{% extends 'base.html.twig' %}
{% block body %}
    {% embed 'menu.html.twig' %}
        {% block nav %}
            <li class="nav-item" wfd-id="30">
                <a href="" class="btn btn-vicsa-indigo" id="create" data-target="#purchaseModal" data-toggle="modal">Agregar Compra | <i class="fa fa-plus"></i></a> 
            </li>
        {% endblock %}
        {% block content %}
            <table id="dtBasicExample" class="table table-striped table-bordered table-sm" cellspacing="0" width="100%">
                <thead>
                    <tr>
                        <th class="th-sm">Nombre
                            <i class="fa fa-sort float-right" aria-hidden="true"></i>
                        </th>
                        <th class="th-sm">Fecha de entrada
                            <i class="fa fa-sort float-right" aria-hidden="true"></i>
                        </th>
                    </tr>
                </thead>
                <tbody>
                    {% for purch in purchase %}
                        <tr>
                            <td><a href="#">{{ purch.description }}</a></td>
                            <td>{{ purch.entryDate | date("m/d/Y") }}</td>
                        </tr>
                    {% endfor %}
                </tbody>
            </table>
        {% endblock %}
    {% endembed %}
    <div class="modal fade" id="purchaseModal" tabindex="-1" role="dialog">
        <div class="modal-dialog modal-dialog-centered modal-lg" role="document">
            <div class="modal-content">
                <div class="modal-header" id="modalHeader">
                    <h5 class="modal-title">Agregar Compras</h5>
                </div>
                <div class="modal-body">
                    {{ form_start(formFiles, {'attr': {'id': 'files'}}) }}
                    {{ form_row(formFiles.files) }}

                </div>
                <div class="modal-footer">
                    <button type="button" class="btn btn-secondary" data-dismiss="modal">Cancelar</button>
                    {{ form_row(formFiles.submit) }}
                    {{ form_end(formFiles) }}
                </div>
            </div>
        </div>
    </div>
{% endblock %}
{% block javascripts %}
    <script>
        $(document).ready(function () {
            $('#dtBasicExample')
                    .addClass('nowrap')
                    .dataTable({
                        responsive: true,
                        columnDefs: [
                            {targets: [-1], className: 'dt-body-right'}
                        ]
                    });
        });
    </script>

{% endblock %}